[pytest]
markers =
    slow: tests that load ML models (semantic/hybrid search)
testpaths = tests
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated; install `httpx2` instead\.:DeprecationWarning
